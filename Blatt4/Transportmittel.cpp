#include <iostream>
#include <string>
#include <cstring>
#include "Transportmittel.h"
using namespace std;

//allgemeiner Konstruktor der Basisklasse
transportmittel(double hoehe, double breite);
//Methode
void bewegen(string richtung)
{
	x = richtung
		if (x = norden)
		{
			cout << "in Richtung" << x << "bewegen" << endl;
		}
		if (x = süden)
		{
			cout << "in Richtung" << x << "bewegen" << endl;
		}
		if (x = osten)
		{
			cout << "in Richtung" << x << "bewegen" << endl;
		}
		if (x = westen)
		{
			cout << "in Richtung" << x << "bewegen" << endl;
		}
}
//allgemeiner Konstruktor der Klasse Wassertransportmittel
wassertransportmittel(double bruttoregistertonnen, double hoehe, double breite);
//Mathoden
void fahren(double km)
{
	return km;
}
void schieben(double Km)
{
	return Km
}
void anlegen(string anlegehafen)
{
	return anlegehafen;
}
void ablegen(string ablegehafen)
{
	return ablegehafen;
}
